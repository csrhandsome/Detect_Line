conda create -n device python=3.9
conda activate device
# 安装ultralytics
pip install ultralytics

# 安装其他可能需要的包
pip install torch torchvision
pip install Pillow
pip install matplotlib
pip install scikit-learn